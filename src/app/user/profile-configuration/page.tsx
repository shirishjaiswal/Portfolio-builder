"use server";

import ProfileConfigurationMain from "@/components/app/user/profile-configuration/profile-configuration-main";

const Page: React.FC = () => {
  return <ProfileConfigurationMain />;
};

export default Page;